# Fix Git hook output display issues (for Git 2.36.0+)
if sh -c ": >/dev/tty" >/dev/null 2>/dev/null; then exec >/dev/tty 2>&1; fi

# Run lint-staged (includes formatting and linting of changed files)
pnpm lint-staged

# Run TypeScript type checking
pnpm type-check

# Run tests
pnpm test

# Run production build
pnpm build

# Note: Full build check moved to CI/CD
# Run 'pnpm ai-check && pnpm build' manually before releases
